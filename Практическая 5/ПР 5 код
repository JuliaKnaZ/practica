Вариант 1.
while True:
    N = int(input("Введите число N (или 0 для выхода): "))

    if N == 0:
        print("Программа завершена.")
        break  # Выход из цикла при вводе 0

    i = 1  # Начальное значение счетчика

    # Цикл продолжается, пока квадрат числа i не превышает N
    while i * i <= N:
        print(i * i, end=" ")  # Вывод квадрата числа
        i += 1  # Увеличиваем счетчик

    print("\n")  # Перевод строки после вывода всех квадратов
